# This is a Databricks asset bundle definition for ingesta_s3_brz.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: ingesta_s3_brz
  uuid: 430de1fd-98a1-4ebd-bbc4-95e4834edf11

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-dfb14324-e95d.cloud.databricks.com
    variables:
      catalog: bronze_dev
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: https://dbc-dfb14324-e95d.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/daniel.epr77@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/datawizard_dlh/_bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: bronze
      schema: prod
    permissions:
      - user_name: daniel.epr77@gmail.com
        level: CAN_MANAGE
